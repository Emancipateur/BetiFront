import React from 'react';

const calculPriceForRooms = (day, room, id, selectedDate) => {

    console.log(day, room, id, selectedDate);



    return (
        <div>
            
        </div>
    );
};

export default calculPriceForRooms;